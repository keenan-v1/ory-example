# WORK IN PROGRESS
# DOES NOT WORK YET
name: Database Provisioner

on:
  workflow_call:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-to-ecs-fargate:
    runs-on: ubuntu-latest
    steps:
      - name: TODO
        run: |
          echo "TODO"
